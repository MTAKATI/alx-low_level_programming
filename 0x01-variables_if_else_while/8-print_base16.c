#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;
	char i1;

	for (i = 0; i <= 9; i++)
		putchar(i);

	for (i1 = 'a'; i1 <= 'f'; i1++)
		putchar(i1);

	return (0);

}
